{
  "name": "loggerts",
  "version": "1.0.1",
  "description": "Simple logger using TS",
  "license": "ISC",
  "author": "DiEmmal",
  "type": "module",
  "main": "logger.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:build": "rimraf dist && tsc",
    "start:demo": "node dist/logger.demo.js",
    "dev:demo": "tsx src/logger.demo.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "rimraf": "^6.1.3",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  }
}
