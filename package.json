{
  "name": "loggerts",
  "version": "1.1.0",
  "description": "Simple logger using TS",
  "license": "ISC",
  "author": "DiEmmal",
  "type": "module",
  "main": "./dist/logger.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "rimraf": "^6.1.3",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  }
}
